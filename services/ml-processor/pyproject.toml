[project]
name = "ml-processor"
version = "0.1.0"
description = "ML backend for AI Technical Clipper: ingestion, transcription, and reframing"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
